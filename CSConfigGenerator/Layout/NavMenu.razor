<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">CSConfigGenerator</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="nav flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="config/player">
                <span class="bi bi-person-fill-nav-menu" aria-hidden="true"></span> Player Config
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="config/server">
                <span class="bi bi-hdd-stack-fill-nav-menu" aria-hidden="true"></span> Server Config
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="commandreference">
                <span class="bi bi-book-half-nav-menu" aria-hidden="true"></span> Command Reference
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
